<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>cars r us</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
  {{!--
  <link rel="stylesheet" type="text/css" href="/css/jass.css"> --}}
  <link rel="stylesheet" type="text/css" href="/css/style.css">

  <script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"></script>
</head>
<style>
  :root {
  --primary: #112B3C;
  --secondary: #205375;
  --tertiary: #F66B0E;
  --quatrinary: #dcd9d9;
  --light: #e3e4e3;
  --dark: #182c23;
  --error: rgb(228, 46, 1);
  --border-radius: 0.3rem;
}

html {
    height: 100%;
  }


body {
    font-family: Arial, Helvetica, sans-serif;
    margin: 0;
    padding: 0;
    background-color:var(--quatrinary);
}

* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}


h1,h2,h3,h4,h5,h6{
    font-size: 100%;
}

 a {
    color: rgba(62, 62, 63, 1);
    text-decoration: none;
 }

 input {
     display: block;
 }

 ul {
     list-style-type:  none;
     background: #fff;
 }

.wrapper {
  width: 100%;
  min-height: 100%;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  min-height: 100vh;
  justify-content: flex-start;
  background-color:var(--quatrinary);
}



main.container {
  max-height: 70vh;
  height: 70vh;
  overflow: hidden;
}


a:visited {
  color: inherit;
}


nav {
  display: flex;
  justify-content: flex-end;
  height: 10vh;
  width: 95%;
  margin: 0 auto;
} 

.logo{
  color: #F66B0E;
  font-weight: 700;
  padding: 10px;
  margin: 10px 10px;
  width: 100px;
  height: 40px;
  display: inline-block;
}

.navbar-nav{

margin-right: 15px;
font-size: 18px;
display: flex;
gap: 1em;
text-align: end;
}

.nav-item{

  pad: 5px 14px;
}
.nav-link{
  color: var(--light);
}
nav a {

  padding: 14px 20px;
  text-decoration: none;
  text-align: center;
}

nav a:hover {

  background-color: var(--dark);
}


/* Changes the background color and font color of the login-form and signup-form from login.js. */
.login-form {
  background-color: var(--primary);
  color: var(--light);
}

.signup-form {
  background-color: var(--primary);
  color: var(--light);
}

/* Changes the background color and font color of the username-signup and password-signup from login.js. */
#username-signup {
  background-color: var(--light);
  color: var(--tertiary);
}

#password-signup {
  background-color: var(--light);
  color: var(--tertiary);
}

/* Changes the background color and font color of the username-login and password-login from login.js. */
#username-login {
  background-color: var(--light);
  color: var(--tertiary);
}

#password-login {
  background-color: var(--light);
  color: var(--tertiary);
}

/* Changes the background color and font color of the logout button from logout.js. As well as changing those colors while hovering the button. */
button {
  background-color: var(--secondary);
  color: var(--light);
}

button:hover {
  color: var(--secondary);
  background-color: var(--dark);
}


/* footer */
footer{
  color:var(--quatrinary);
  height: auto;
  padding: 30px 0;
}

footer #footer-nav-container{
   color:var(--quatrinary);
  display: block;
  text-align: center;
}

#footer-nav-container div{
  display: inline-block;
}

footer a{
   color:var(--quatrinary);
  margin-right: 0;
  padding: 0 1.5em;
}


footer .logo {
  margin-top: 2rem;
}


</style>

<body>

    <header class="row">

 <nav class="navbar navbar-expand-lg bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand logo" href="about-us">Car-R-Us</a>
    <button class="navbar-toggler bg-light" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav bg-dark me-auto mb-2 mb-lg-0">
        <li class="nav-item bg-dark">
          <a class="nav-link" href="about-us">About Us</a>
        </li>
        <li class="nav-item bg-dark">
          <a class="nav-link" href="/">Buy</a>
        </li>
         <li class="nav-item bg-dark">
          <a class="nav-link" href="/dashboard">Sale</a>
        </li>
         {{#if loggedIn}}
          <li class="nav-item bg-dark"><a class="nav-link" href="/" id="logout">Logout</a></li>
          {{else}}
          <li class="nav-item bg-dark"><a class="nav-link" href="/login">Login</a></li>

          {{/if}}
          <li class="nav-item bg-dark"><a class="nav-link" href="/contact-us">Contact Us</a></li>
           <li class="nav-item bg-dark"><a class="nav-link" href="/">About Us</a></li>
       
      </ul>
      <form class="d-flex" role="search">
        <input class="form-control me-2" type="search" placeholder="Search by Model" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>
      </header>
      <div class="container wrapper">
    <main>
      <div class="gallery">
        {{{body}}}content
      </div>
    </main>
  </div>
 <footer class="bg-dark">
      <div id="footer-nav-container">
        <div>
          {{#if loggedIn}}
              <a class="logout" href="" class="product">Log Out</a>
            {{else}}
              <a href="/login" class="product">Log In</a>
            {{/if}}
          <a class="privacy-policy" href="/privacy-policy">Privacy Policy</a>
          <a class="contact" href="/contact-us">Contact Us</a>
       
          <br />
          <p>&copy; 2022 Cars-R-Us. All rights reserved</p>
        </div>
        <div class="logo">
          
          Cars-R-Us
          </div>
        </div>
      </div>
    </footer>
  
</body>
{{#if loggedIn}}
<script src="/js/logout.js"></script>
{{/if}}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js"
  integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>


</html>